# Modern Editorial Blue

유리나무 작업 허브(YURIWOOD WORK HUB)의 디자인 기준 문서입니다.
**앞으로 이 프로젝트의 UI를 수정할 때는 이 문서를 기준으로 작업합니다.** 색상·여백·타이포그래피를 바꿔야 할 때는 임의로 값을 추가하지 말고 이 문서를 먼저 갱신한 뒤 코드에 반영합니다.

## 디자인 방향

스위스 스타일을 기반으로 한 **모던 에디토리얼 대시보드**입니다.
창작자 포트폴리오와 업무 대시보드의 중간 느낌을 목표로 합니다. 장식이 아니라 정보와 기능이 화면을 이끌고, 밝은 지면 위에 흰 카드와 얇은 테두리, 절제된 블루와 아주 적은 코럴 포인트를 얹는 구성입니다.

- 시원하고 넓은 여백
- 정돈된 그리드와 명확한 정보 위계
- 굵고 또렷한 제목
- 흰 카드 + 1px 테두리 + 아주 약한 그림자

## 색상

모든 색상은 `styles.css`의 `:root` CSS 변수로만 관리합니다. 값을 직접 하드코딩하지 않습니다.

| 용도 | 값 | 변수 |
| --- | --- | --- |
| 페이지 배경 | `#F7F8FA` | `--color-bg` |
| 카드·패널 배경 (사이드바 포함) | `#FFFFFF` | `--color-surface` |
| 큰 제목·핵심 텍스트 | `#1A273A` | `--color-ink` |
| 본문·보조 텍스트 | `#3E4A62` | `--color-ink-soft` |
| 기본 블루 (버튼·선택·링크·포커스) | `#446592` | `--color-primary` |
| 버튼 호버 | `#38557C` | `--color-primary-hover` |
| 연한 블루 | `#ACE5F6` | `--color-primary-soft` |
| 코럴 포인트 | `#C24D2C` | `--color-accent` |
| 구분선·테두리 | `#D9D9D7` | `--color-border` |
| 약한 그림자 | `0 8px 24px rgba(26,39,58,0.08)` | `--shadow-soft` |
| 호버 그림자 | `0 12px 30px rgba(26,39,58,0.12)` | `--shadow-hover` |

### 파생값

연한 블루는 원색 그대로 넓게 깔지 않고, 흰 배경 위에 낮은 농도로만 얹습니다.

| 용도 | 값 | 변수 |
| --- | --- | --- |
| 선택 메뉴·선택 카드 배경 | `rgba(172,229,246,0.30)` | `--tint-soft` |
| 포커스 링·작은 정보 배지 | `rgba(172,229,246,0.55)` | `--tint` |
| 메뉴 호버 (중성) | `rgba(26,39,58,0.045)` | `--hover-neutral` |
| 모바일 드로어 스크림 | `rgba(26,39,58,0.42)` | `--scrim` |

기존 변수 이름(`--bg`, `--card`, `--ink`, `--ink-soft`, `--line`, `--blue`, `--blue-soft`, `--sidebar-*`)은 위 팔레트를 가리키도록 연결만 되어 있습니다. 새 색을 넣을 때는 `--color-*`를 고치고 파생 변수를 따라가게 합니다.

### 기본 블루 `#446592`를 쓰는 곳

- 주요 버튼 배경 (**버튼 위 글자는 흰색**), 호버는 `#38557C`
- 링크와 외부 링크 화살표의 기본 색
- 카드·패널 호버와 선택 시의 테두리
- 포커스 테두리와 검색창 포커스 테두리
- 영문 보조 제목, 사이트 개수 표시
- 브랜드 마크 배경

### 연한 블루 `#ACE5F6`을 쓰는 곳

낮은 농도(`--tint-soft` / `--tint`)로만 씁니다.

- 선택된 메뉴의 배경
- 검색창 포커스 링
- 런처 카드 선택 배경, 런처 카드의 작은 정보 배지
- 카드 호버 보조색

**연한 블루 위에는 흰 글자를 쓰지 않습니다.** `#1A273A` 또는 `#3E4A62`만 올립니다.

### 코럴 `#C24D2C`을 쓰는 곳 — 화면의 5% 이하

작은 표식에만 씁니다. 현재 코드에서 코럴이 쓰이는 곳은 다음 7군데뿐입니다.

- 런처 카드 번호와 상세 패널 번호
- 선택된 상위 메뉴 왼쪽의 3px 표시
- 선택된 하위 메뉴 왼쪽의 3px 표시
- 선택된 런처 카드 아래의 3px 표시
- 사이트 카드·도구 카드 호버 시의 외부 링크 화살표

다음에는 쓰지 않습니다.

- 카드 전체 배경, 넓은 페이지 배경
- 긴 본문 텍스트
- 모든 버튼, 모든 카테고리

### 금지

- 카테고리마다 다른 강한 원색을 쓰는 무지개식 색 구분
- 과도한 그라데이션, 네온 컬러
- 두꺼운 그림자, 유리(글래스모피즘) 효과
- 지나치게 둥근 카드 (반경은 카드 14px, 작은 요소 10px)
- 검은색을 넓은 면적으로 사용
- 코럴 외의 추가 포인트 색상

## 레이아웃과 타이포그래피

- 넓은 여백과 규칙적인 그리드. 카드는 `auto-fill` 그리드로 데스크톱 3열 → 태블릿 2열 → 좁은 화면 1열.
- 강한 타이포그래피 위계: 큰 산세리프 제목 → 카테고리 제목 → 카드 제목 → 보조 설명 → 태그. 굵기보다 크기·간격·색으로 위계를 만듭니다.
- 구분선은 1px 헤어라인(`--line`) 하나만 사용합니다.
- 카드 모서리 반경 16px, 카드 높이와 내부 여백은 일정하게 정렬합니다.
- 한글 가독성을 위해 Pretendard를 우선 사용하고, 실패 시 시스템 한글 폰트로 대체합니다.

### 화면 골격

| 영역 | 내용 |
| --- | --- |
| 사이드바 (흰 배경, 오른쪽 1px 테두리, 고정) | 브랜드 → 상위 메뉴 7개(대시보드 + 1차 카테고리 6개) → 아코디언 하위 메뉴 |
| 상단바 (고정, `position: sticky`) | 햄버거(모바일) · 통합 검색창 · 오늘 날짜 |
| 본문 | 브레드크럼 → 마스트헤드(현재 화면 이름·사이트 개수) → **작업 런처** → 카테고리 블록 + 카드 그리드 |
| 푸터 | 브랜드 · 전체 집계 (밝은 지면 유지. 어두운 푸터가 필요하면 `#1A273A` 배경 + 흰 글자) |

### 사이드 메뉴

- 배경은 흰색, 기본 글자는 `#3E4A62`, 선택 메뉴 글자는 `#1A273A`.
- 선택 상태는 **연한 블루 배경 + 글자 굵기 600 + 왼쪽 3px 코럴 표시**를 함께 씁니다. 색 하나로만 알리지 않습니다.
- 메뉴 구분선은 `#D9D9D7`, 호버는 중성 회색(`--hover-neutral`).

### 사이트 카드

카드 안의 순서는 항상 다음으로 고정합니다.

1. 카테고리 경로 (`1차 › 2차`, 11px 대문자 트래킹, `--ink-soft`)
2. 사이트 이름 (21px, 600)
3. 한 줄 설명 (14px, `--ink-soft`)
4. 검색 태그 (`#태그`, 12px, `--ink-soft`) — 정보 보조용이며 배지·칩 형태로 만들지 않습니다
5. 바로가기 표시 (13px + `↗`)

사이트 카드 호버는 테두리를 `#446592`로 바꾸고, `translateY(-2px)`만큼만 살짝 올리며 `--shadow-soft`를 얹습니다. 배경색은 바꾸지 않습니다.

### 검색창

- 상단바 안에 두어 어떤 화면에서도 항상 보이게 합니다.
- 흰 배경 + `#D9D9D7` 1px 테두리, 모서리 반경 10px, 최대 폭 520px.
- 포커스가 들어오면 테두리는 `#446592`, 포커스 링은 연한 블루 3px. 확대 애니메이션은 쓰지 않습니다.
- 라벨은 시각적으로 숨기고 스크린 리더에는 남깁니다. 지우기 버튼에는 `aria-label="검색어 지우기"`를 붙입니다.

### 작업 런처

`전체 사이트` 화면에서 검색창 아래, 사이트 목록 위에 놓입니다.

- 카드는 실제 `button` 요소이며, 안쪽 순서는 번호(코럴) → 제목(`#1A273A`) → 설명(`#3E4A62`) → 작은 정보 배지(연한 블루)입니다.
- 이모지를 쓰지 않습니다.
- 호버는 테두리 `#446592` + `--shadow-soft`.
- 선택 상태는 **테두리 `#446592` + 안쪽 1px 테두리(굵기 효과) + 연한 블루 배경 + 아래쪽 3px 코럴 표시**를 함께 씁니다.
- 상세 패널은 항상 하나이며 카드 그리드 바로 아래에 놓습니다. 사이트 카드와 같은 흰 패널·테두리를 쓰되 테두리만 `#446592`로 두어 지금 열려 있음을 알립니다.
- 작업 단계는 `flex-wrap`으로 넓은 화면에서 가로로 흐르고 220px 아래로 좁아지면 줄바꿈, 620px 이하에서는 세로 한 줄씩 쌓입니다.
- 진행률·체크 기능은 넣지 않습니다.

### 결과 없음 화면

카드와 같은 흰 패널·헤어라인·반경 16px을 쓰고, 가운데 정렬로 제목 → 안내 문장 → `검색 초기화` 버튼만 둡니다. 일러스트나 이모지는 넣지 않습니다.

## 반응형 기준

| 분기 | 기준 | 동작 |
| --- | --- | --- |
| 넓은 데스크톱 | 1400px 이상 | 런처 4열 |
| 데스크톱 | 1201px 이상 | 사이드바 288px 고정, 사이트 카드 3열, 런처 3열(1000px~), 도구 3열(1100px~) |
| 태블릿 | 1200px 이하 | 사이드바 236px로 축소, 사이트 카드 2열 |
| 모바일 | 900px 이하 | 사이드바가 드로어로 전환(햄버거·스크림·`ESC`), 검색창이 상단바 두 번째 줄에서 전체 폭 차지 |
| 좁은 화면 | 620px 이하 | 사이트 카드·런처·도구 모두 1열, 카드 최소 높이 해제, 작업 단계 세로 1열, 런처 패널 머리글 세로 정렬 |
| 초소형 | 400px 이하 | 마스트헤드 제목 축소, 햄버거 글자 숨김(아이콘만) |

확인 기준 폭: **1440 / 1024 / 768 / 390px**. 각 폭에서 가로 스크롤, 카드 겹침, 잘린 제목, 화면 밖으로 나가는 런처 패널이 없어야 합니다.

- 폭 관련 값은 `clamp()`로 이어지게 하고, 분기마다 새 색상이나 새 컴포넌트를 만들지 않습니다.
- 가로 스크롤이 생기지 않아야 합니다. 긴 URL·이름은 `overflow-wrap`으로 줄바꿈합니다.

## 금지 사항

- 그라데이션, 네온 컬러
- 강한 그림자, 유리(글래스모피즘) 효과
- 캡슐형 라운드 버튼 남발, 지나치게 둥근 카드
- 카테고리마다 다른 원색을 쓰는 무지개식 색 구분
- 기본 블루·연한 블루·코럴 외의 추가 포인트 색상
- 의미 없는 장식 요소, 가짜 버튼, 불필요한 이모지
- 장식 목적의 애니메이션
- 검은색을 넓은 면적으로 사용
- 지나치게 작은 글씨와 빽빽하게 잘게 나뉜 카드

## 접근성 기준

- 본문 텍스트는 WCAG **AA(4.5:1)** 이상을 유지합니다. 현재 팔레트의 실측값은 아래와 같습니다.

| 조합 | 대비 |
| --- | --- |
| `#1A273A` on `#F7F8FA` | 14.16:1 |
| `#3E4A62` on `#F7F8FA` | 8.37:1 |
| `#3E4A62` on `#FFFFFF` | 8.90:1 |
| `#446592` on `#FFFFFF` | 5.96:1 |
| `#C24D2C` on `#FFFFFF` | 4.78:1 |
| `#FFFFFF` on `#446592` | 5.96:1 |
| `#1A273A` on 연한 블루 배경 | 13.67:1 |

- 보조 글자를 더 연하게 만들지 않습니다. 흐리게 보이면 명도를 낮춰 대비를 올립니다.
- **선택 상태를 색상만으로 표현하지 않습니다.** 굵기·테두리·위치 표시(코럴 바)를 항상 함께 씁니다.
- 포커스 상태는 `#446592` 아웃라인으로 항상 보이게 유지합니다. `outline: none`으로 지우지 않습니다.
- 런처 카드는 `button` 요소로 만들고 `aria-expanded`와 `aria-controls`로 상세 패널과 연결합니다. 패널에는 고유 id를 하나만 두고, 닫기 버튼에는 `aria-label`로 어떤 런처를 닫는지 밝힙니다.
- 같은 화면에 중복 DOM id를 만들지 않습니다.
- 메뉴·버튼·링크의 터치 영역은 최소 44px 높이를 확보합니다.
- 아이콘만 있는 요소(햄버거, 검색 지우기 버튼)에는 `aria-label` 또는 보이는 글자로 접근 가능한 이름을 반드시 붙입니다. 장식용 도형에는 `aria-hidden="true"`를 붙입니다.
- 외부 링크는 `target="_blank"` + `rel="noopener noreferrer"`를 쓰고, `aria-label`에 "새 탭"임을 밝힙니다.
- 화면이 바뀌는 본문 영역은 `aria-live="polite"`로 두어 검색 결과 변화가 전달되게 합니다.
- `prefers-reduced-motion` 설정을 존중합니다.
