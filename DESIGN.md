# Airy Cobalt Editorial

유리나무 작업 허브(YURIWOOD WORK HUB)의 디자인 기준 문서입니다.
**앞으로 이 프로젝트의 UI를 수정할 때는 이 문서를 기준으로 작업합니다.** 색상·여백·타이포그래피를 바꿔야 할 때는 임의로 값을 추가하지 말고 이 문서를 먼저 갱신한 뒤 코드에 반영합니다.

## 디자인 방향

스위스 스타일을 기반으로 한 **미니멀 에디토리얼 대시보드**입니다.
장식이 아니라 정보와 기능이 화면을 이끌고, 차가운 블루그레이 지면 위에 딥 네이비 사이드바와 절제된 코발트 포인트를 얹는 구성입니다.

## 색상

모든 색상은 `styles.css`의 `:root` CSS 변수로만 관리합니다. 값을 직접 하드코딩하지 않습니다.

| 용도 | 값 | 변수 |
| --- | --- | --- |
| 전체 배경 | `#F4F7FB` | `--bg` |
| 카드·콘텐츠 패널 배경 | `#FFFFFF` | `--card` |
| 사이드바 배경 | `#182033` | `--sidebar-bg` |
| 기본 글자 (제목·주요 정보) | `#18202C` | `--ink` |
| 보조 글자 (설명·부가 정보) | `#687386` | `--ink-soft` |
| 포인트 코발트 | `#5367D9` | `--blue` |
| 선택 메뉴·연한 포인트 배경 | `#E8ECFF` | `--blue-soft` |
| 구분선·카드 테두리 | `#DCE2EC` | `--line` |

### 딥 네이비 사이드바 위의 파생값

어두운 배경에서 대비를 확보하기 위한 값이며, 위 팔레트에서 파생된 중성 톤만 사용합니다.

| 용도 | 값 | 변수 |
| --- | --- | --- |
| 사이드바 기본 글자 (브랜드·상위 메뉴) | `#FFFFFF` | `--sidebar-ink` |
| 사이드바 하위 메뉴 글자 | `#C6CDDB` | `--sidebar-ink-mid` |
| 사이드바 보조 글자·숫자 | `#A3AEC2` | `--sidebar-ink-soft` |
| 사이드바 구분선 | `rgba(255,255,255,0.14)` | `--sidebar-line` |
| 사이드바 호버 배경 | `rgba(232,236,255,0.12)` | `--sidebar-hover` |
| 모바일 드로어 스크림 | `rgba(24,32,44,0.42)` | `--scrim` |

보조 글자가 흐리게 보이면 다른 색상 계열로 바꾸지 말고, 같은 블루그레이 계열에서 명도만 낮춰 대비를 확보합니다.

### 코발트를 쓰는 곳

`#5367D9`는 다음에만 절제해서 사용합니다.

- 선택된 상위·하위 메뉴 (배경 `#E8ECFF` + 글자·아이콘 `#5367D9`)
- 링크와 카드 호버 (호버 배경은 `#E8ECFF`)
- 포커스 테두리, 검색창 포커스 테두리
- 사이트 개수 표시
- 현재 카테고리 표시
- 검색 초기화 버튼

## 레이아웃과 타이포그래피

- 넓은 여백과 규칙적인 그리드. 카드는 `auto-fill` 그리드로 데스크톱 3열 → 태블릿 2열 → 좁은 화면 1열.
- 강한 타이포그래피 위계: 큰 산세리프 제목 → 카테고리 제목 → 카드 제목 → 보조 설명 → 태그. 굵기보다 크기·간격·색으로 위계를 만듭니다.
- 구분선은 1px 헤어라인(`--line`) 하나만 사용합니다.
- 카드 모서리 반경 16px, 카드 높이와 내부 여백은 일정하게 정렬합니다.
- 한글 가독성을 위해 Pretendard를 우선 사용하고, 실패 시 시스템 한글 폰트로 대체합니다.

### 화면 골격

| 영역 | 내용 |
| --- | --- |
| 사이드바 (딥 네이비, 고정) | 브랜드 → 상위 메뉴 7개(대시보드 + 1차 카테고리 6개) → 아코디언 하위 메뉴 |
| 상단바 (고정, `position: sticky`) | 햄버거(모바일) · 통합 검색창 · 오늘 날짜 |
| 본문 | 브레드크럼 → 마스트헤드(현재 화면 이름·사이트 개수) → 카테고리 블록 + 카드 그리드 |
| 푸터 | 브랜드 · 전체 집계 |

### 사이트 카드

카드 안의 순서는 항상 다음으로 고정합니다.

1. 카테고리 경로 (`1차 › 2차`, 11px 대문자 트래킹, `--ink-soft`)
2. 사이트 이름 (21px, 600)
3. 한 줄 설명 (14px, `--ink-soft`)
4. 검색 태그 (`#태그`, 12px, `--ink-soft`) — 정보 보조용이며 배지·칩 형태로 만들지 않습니다
5. 바로가기 표시 (13px + `↗`)

### 검색창

- 상단바 안에 두어 어떤 화면에서도 항상 보이게 합니다.
- 흰 배경(`--card`) + 1px 헤어라인, 모서리 반경 10px, 최대 폭 520px.
- 포커스가 들어오면 테두리만 코발트로 바뀝니다. 그림자나 확대 애니메이션은 쓰지 않습니다.
- 라벨은 시각적으로 숨기고 스크린 리더에는 남깁니다. 지우기 버튼에는 `aria-label="검색어 지우기"`를 붙입니다.

### 결과 없음 화면

카드와 같은 흰 패널·헤어라인·반경 16px을 쓰고, 가운데 정렬로 제목 → 안내 문장 → `검색 초기화` 버튼만 둡니다. 일러스트나 이모지는 넣지 않습니다.

## 반응형 기준

| 분기 | 기준 | 동작 |
| --- | --- | --- |
| 데스크톱 | 1201px 이상 | 사이드바 288px 고정, 카드 3열 |
| 태블릿 | 1200px 이하 | 사이드바 236px로 축소, 카드 2열 |
| 모바일 | 900px 이하 | 사이드바가 드로어로 전환(햄버거·스크림·`ESC`), 검색창이 상단바 두 번째 줄에서 전체 폭 차지 |
| 좁은 화면 | 620px 이하 | 카드 1열, 카드 최소 높이 해제 |
| 초소형 | 400px 이하 | 마스트헤드 제목 축소, 햄버거 글자 숨김(아이콘만) |

- 폭 관련 값은 `clamp()`로 이어지게 하고, 분기마다 새 색상이나 새 컴포넌트를 만들지 않습니다.
- 가로 스크롤이 생기지 않아야 합니다. 긴 URL·이름은 `overflow-wrap`으로 줄바꿈합니다.

## 금지 사항

- 그라데이션
- 강한 그림자, 유리(글래스모피즘) 효과
- 캡슐형 라운드 버튼 남발
- 코발트 외의 추가 포인트 색상
- 의미 없는 장식 요소, 가짜 버튼, 불필요한 이모지
- 지나치게 작은 글씨와 빽빽하게 잘게 나뉜 카드

## 접근성 기준

- 본문 텍스트와 배경의 대비를 충분히 확보하고, 사이드바의 비선택 메뉴도 어두운 배경에서 또렷하게 읽혀야 합니다.
- 포커스 상태는 `#5367D9` 아웃라인으로 항상 보이게 유지합니다. `outline: none`으로 지우지 않습니다.
- 메뉴·버튼·링크의 터치 영역은 최소 44px 높이를 확보합니다.
- 아이콘만 있는 요소(햄버거, 검색 지우기 버튼)에는 `aria-label` 또는 보이는 글자로 접근 가능한 이름을 반드시 붙입니다. 장식용 도형에는 `aria-hidden="true"`를 붙입니다.
- 외부 링크는 `target="_blank"` + `rel="noopener noreferrer"`를 쓰고, `aria-label`에 "새 탭"임을 밝힙니다.
- 화면이 바뀌는 본문 영역은 `aria-live="polite"`로 두어 검색 결과 변화가 전달되게 합니다.
- `prefers-reduced-motion` 설정을 존중합니다.
