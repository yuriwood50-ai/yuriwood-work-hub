# Airy Cobalt Editorial

유리나무 작업 허브(YURIWOOD WORK HUB)의 디자인 기준 문서입니다.
**앞으로 이 프로젝트의 UI를 수정할 때는 이 문서를 기준으로 작업합니다.** 색상·여백·타이포그래피를 바꿔야 할 때는 임의로 값을 추가하지 말고 이 문서를 먼저 갱신한 뒤 코드에 반영합니다.

## 디자인 방향

스위스 스타일을 기반으로 한 **미니멀 에디토리얼 대시보드**입니다.
장식이 아니라 정보와 기능이 화면을 이끌고, 차가운 블루그레이 지면 위에 딥 네이비 사이드바와 절제된 코발트 포인트를 얹는 구성입니다.

## 색상

모든 색상은 `styles.css`의 `:root` CSS 변수로만 관리합니다. 값을 직접 하드코딩하지 않습니다.

| 용도 | 값 | 변수 |
| --- | --- | --- |
| 전체 배경 | `#F4F7FB` | `--bg` |
| 카드·콘텐츠 패널 배경 | `#FFFFFF` | `--card` |
| 사이드바 배경 | `#182033` | `--sidebar-bg` |
| 기본 글자 (제목·주요 정보) | `#18202C` | `--ink` |
| 보조 글자 (설명·부가 정보) | `#687386` | `--ink-soft` |
| 포인트 코발트 | `#5367D9` | `--blue` |
| 선택 메뉴·연한 포인트 배경 | `#E8ECFF` | `--blue-soft` |
| 구분선·카드 테두리 | `#DCE2EC` | `--line` |

### 딥 네이비 사이드바 위의 파생값

어두운 배경에서 대비를 확보하기 위한 값이며, 위 팔레트에서 파생된 중성 톤만 사용합니다.

| 용도 | 값 | 변수 |
| --- | --- | --- |
| 사이드바 기본 글자 (브랜드·상위 메뉴) | `#FFFFFF` | `--sidebar-ink` |
| 사이드바 하위 메뉴 글자 | `#C6CDDB` | `--sidebar-ink-mid` |
| 사이드바 보조 글자·숫자 | `#A3AEC2` | `--sidebar-ink-soft` |
| 사이드바 구분선 | `rgba(255,255,255,0.14)` | `--sidebar-line` |
| 사이드바 호버 배경 | `rgba(232,236,255,0.12)` | `--sidebar-hover` |
| 모바일 드로어 스크림 | `rgba(24,32,44,0.42)` | `--scrim` |

보조 글자가 흐리게 보이면 다른 색상 계열로 바꾸지 말고, 같은 블루그레이 계열에서 명도만 낮춰 대비를 확보합니다.

### 코발트를 쓰는 곳

`#5367D9`는 다음에만 절제해서 사용합니다.

- 선택된 상위·하위 메뉴 (배경 `#E8ECFF` + 글자·아이콘 `#5367D9`)
- 링크와 카드 호버 (호버 배경은 `#E8ECFF`)
- 포커스 테두리
- 사이트 개수 표시
- 현재 카테고리 표시

## 레이아웃과 타이포그래피

- 넓은 여백과 규칙적인 그리드. 카드는 `auto-fill` 그리드로 데스크톱 3열 → 태블릿 2열 → 좁은 화면 1열.
- 강한 타이포그래피 위계: 큰 산세리프 제목 → 카테고리 제목 → 카드 제목 → 보조 설명. 굵기보다 크기·간격·색으로 위계를 만듭니다.
- 구분선은 1px 헤어라인(`--line`) 하나만 사용합니다.
- 카드 모서리 반경 16px, 카드 높이와 내부 여백은 일정하게 정렬합니다.
- 한글 가독성을 위해 Pretendard를 우선 사용하고, 실패 시 시스템 한글 폰트로 대체합니다.

## 금지 사항

- 그라데이션
- 강한 그림자, 유리(글래스모피즘) 효과
- 캡슐형 라운드 버튼 남발
- 코발트 외의 추가 포인트 색상
- 의미 없는 장식 요소, 가짜 버튼, 불필요한 이모지
- 지나치게 작은 글씨와 빽빽하게 잘게 나뉜 카드

## 접근성 기준

- 본문 텍스트와 배경의 대비를 충분히 확보하고, 사이드바의 비선택 메뉴도 어두운 배경에서 또렷하게 읽혀야 합니다.
- 포커스 상태는 `#5367D9` 아웃라인으로 항상 보이게 유지합니다.
- 메뉴·버튼·링크의 터치 영역은 최소 44px 높이를 확보합니다.
- `prefers-reduced-motion` 설정을 존중합니다.
